<ul class="messages-list relative top-0">
  <%  Recipient.where(user_id: current_user.id).each do |r|%>
  <% @message = Message.find(r.message_id)%>
  <%= render partial: 'messages/item', locals: {message: @message, user: @message.sender}%>
  <%end%>
</ul>
