$("#friendship").replaceWith("<%= escape_javascript(render(:partial => 'friendships/profile_block', locals: {profile: @profile })) %>");
notif({
    msg: "<%=t :friend_added%>",
    type: "success",
    position: "center"
});
