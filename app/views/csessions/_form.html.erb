<%= form_for(@csession) do |f| %>
  <% if @csession.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@csession.errors.count, "error") %> prohibited this csession from being saved:</h2>

      <ul>
      <% @csession.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :course_id %><br>
    <%= f.number_field :course_id %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
