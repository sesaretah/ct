<%= form_for(@visit) do |f| %>
  <% if @visit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@visit.errors.count, "error") %> prohibited this visit from being saved:</h2>

      <ul>
      <% @visit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :visitable_type %><br>
    <%= f.text_field :visitable_type %>
  </div>
  <div class="field">
    <%= f.label :visitable_id %><br>
    <%= f.number_field :visitable_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
